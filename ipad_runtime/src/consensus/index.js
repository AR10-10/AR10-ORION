// index.js — ponto de entrada único do Consensus Engine (Fase F).
export {
    ENSEMBLE_DIRECTIONS,
    opinionFromLabel,
    opinionFromLean,
    opinionFromVote,
    buildEnsembleConsensus,
} from './ensemble-engine.js';
